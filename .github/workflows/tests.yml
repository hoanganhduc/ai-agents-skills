name: Installer Tests

on:
  pull_request:
    paths:
      - ".github/workflows/tests.yml"
      - "Makefile"
      - "make.bat"
      - "README.md"
      - "docs/**"
      - "installer/**"
      - "tests/**"
      - "tools/**"
      - "manifest/**"
      - "canonical/**"
      - "targets/**"
      - "pyproject.toml"
      - "docs/requirements.txt"
  push:
    paths:
      - ".github/workflows/tests.yml"
      - "Makefile"
      - "make.bat"
      - "README.md"
      - "docs/**"
      - "installer/**"
      - "tests/**"
      - "tools/**"
      - "manifest/**"
      - "canonical/**"
      - "targets/**"
      - "pyproject.toml"
      - "docs/requirements.txt"

permissions:
  contents: read

jobs:
  linux:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run sanitizer checks
        run: make sanitize-check

      - name: Run unit tests
        run: make test

      - name: Verify generated docs are current
        run: |
          make docs
          git diff --exit-code -- README.md docs

      - name: Build docs site
        run: |
          python -m pip install -r docs/requirements.txt
          make docs-site

      - name: Run fake-root lifecycle matrix
        run: make lifecycle-test ARGS="--matrix stress --platform-shape all"

  macos:
    runs-on: macos-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run sanitizer checks
        run: make sanitize-check

      - name: Run unit tests
        run: make test

      - name: Run platform lifecycle smoke
        run: make lifecycle-test ARGS="--matrix default --platform-shape macos"

  windows:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check command help
        shell: cmd
        run: make.bat help

      - name: List artifacts
        shell: cmd
        run: make.bat list-artifacts

      - name: Check make.bat propagates command failures
        shell: cmd
        run: |
          call make.bat definitely-not-a-command
          if not errorlevel 1 exit /b 1
          exit /b 0

      - name: Run sanitizer checks
        shell: cmd
        run: make.bat sanitize-check

      - name: Run unit tests
        shell: cmd
        run: make.bat test

      - name: Run platform lifecycle smoke
        shell: cmd
        run: make.bat lifecycle-test --matrix default --platform-shape windows
