# Manifest Schemas

Schemas will live here once the manifest format stabilizes. The current
implementation validates required fields in Python and keeps manifest files in
JSON-compatible YAML so the bootstrap does not need external dependencies.
