# Canonical Skills

Canonical skill behavior is described in `manifest/skills.yaml`. Per-agent
targets are generated from that manifest so Codex, Claude, and DeepSeek use the
same canonical kebab-case names after migration.
